using System.Collections.Generic;
using UnityEngine;

// this type should be in-sync with the one that PlanMapper serializes
public class FloorIntersectionTarget
{
    public string mapId;
    public string intersectionId;
}